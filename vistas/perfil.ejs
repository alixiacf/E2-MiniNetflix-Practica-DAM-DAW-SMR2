<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Perfil de <%= nombre %> - Mini Netflix</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: <%= usuario.color %>;
      margin: 0;
      padding: 20px;
      text-align: center;
    }
    h1 {
      color: #fff;
    }
    .pelicula {
      display: inline-block;
      margin: 20px;
      padding: 10px;
      background: white;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      width: 250px;
    }
    .pelicula img {
      width: 100%;
      height: 200px;
      object-fit: cover;
      border-radius: 8px;
    }
    .pelicula h3 {
      margin: 10px 0;
    }
  </style>
</head>
<body>
  <h1>¡Hola <%= nombre %>! Estas son tus películas favoritas</h1>
  <% usuario.peliculas.forEach((pelicula, index) => { %>
    <div class="pelicula">
      <img src="/img/<%= usuario.imagenes[index] %>" alt="<%= pelicula %>">
      <h3><%= pelicula %></h3>
    </div>
  <% }); %>
</body>
</html>